# 📄 PDF Extractor – Intelligent Table & Text Parser

A powerful tool to extract structured tables and clean text from PDFs using `unstructured`, `beautifulsoup`, and `sentence-transformers`.

---

## 🚀 Features
- Extracts tables and text chunks from complex PDFs
- Exports to CSV and JSON
- Handles broken text and headers
- CLI-based, easy to use

## 🔧 Installation

```bash
git clone https://github.com/yourusername/Pdf_extractor.git
cd Pdf_extractor
pip install -r requirements.txt
